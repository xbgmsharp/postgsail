# PostgSail Kubernetes

A simple manifest for deploying the PostgSail Server in a Kubernetes environment.

```bash 
kubectl apply -k kubernetes/
```

For full documentation, see [docs/src/run-with-kubernetes.md](../docs/src/run-with-kubernetes.md).
