# SMTP server settings

This is to send email.

PGSAIL_EMAIL_FROM=root@localhost
PGSAIL_EMAIL_SERVER=localhost
#PGSAIL_EMAIL_USER= Comment if not use
#PGSAIL_EMAIL_PASS= Comment if not use

SMTP with TLS

MailGun:
```
PGSAIL_EMAIL_SERVER=smtp.eu.mailgun.org
PGSAIL_EMAIL_USER=postmaster@mail.mydomain.com
PGSAIL_EMAIL_PASS="password"
```
